<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edith.ml2">

    <!-- ML2-specific AR permissions -->
    <uses-permission android:name="com.magicleap.permission.EYE_TRACKING"/>
    <uses-permission android:name="com.magicleap.permission.HAND_TRACKING"/>
    <uses-permission android:name="com.magicleap.permission.SPATIAL_MAPPING"/>
    <uses-permission android:name="com.magicleap.permission.WORLD_CAMERA"/>

    <!-- Standard -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Feature declarations -->
    <uses-feature android:name="com.magicleap.hardware.eye_tracking"  android:required="true"/>
    <uses-feature android:name="com.magicleap.hardware.hand_tracking" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone"          android:required="true"/>

    <application
        android:label="EDITH AR"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
        android:networkSecurityConfig="@xml/network_config"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:immersive="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- ML2 immersive app -->
                <category android:name="com.magicleap.intent.category.IMMERSIVE_3D"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
